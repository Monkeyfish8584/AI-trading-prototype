import{test,expect}from'@playwright/test';test('Monday open golden path',async({page})=>{await page.goto('/');await page.getByLabel('Ask the chart').fill('Mark Monday open');await page.getByText('Apply').click();await expect(page.getByText('Monday weekly open')).toBeVisible();await expect(page.getByRole('status')).toContainText('from 2024');await page.getByText('Undo').click();await expect(page.getByText('Monday weekly open')).toHaveCount(0)});
